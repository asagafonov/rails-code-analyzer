p = "#{@user.nickname},"
p = t('mailers.repository_checks.failed.description')
p = "#{t('mailers.repository_checks.failed.errors_count')}: #{@check.linter_errors_count}"

= link_to t('mailers.repository_checks.failed.link'), repository_check_url(@repo, @check)
